seed: 2026
runtime:
  cache_version: 2
  n_jobs: -1
infrared:
  sample_fps: 1.0
  resize_width: 480
  max_samples: 300
  clahe_clip_limit: 2.0
  motion_percentile: 99.5
  contrast_percentile: 99.7
  min_blob_area: 2
  max_blob_area_ratio: 0.002
  max_track_jump_ratio: 0.15
  max_missed_frames: 4
radar:
  max_numeric_columns: 64
  max_vector_expansion: 8
  target_rate_hz: 5.0
  spectral_bins: 4
alignment:
  enabled: true
  common_rate_hz: 2.0
  max_lag_seconds: 30.0
  estimate_drift: false
  drift_segments: 3
  max_drift_ppm: 5000
window:
  length_seconds: 10.0
  stride_seconds: 10.0
  min_coverage_ratio: 0.40
  max_windows_per_record: 80
model:
  estimator: extra_trees
  n_estimators: 200
  min_samples_leaf: 1
  max_features: sqrt
  class_weight: balanced
  probability_smoothing: 0.02
  branch_weights:
    fusion: 0.55
    radar: 0.30
    infrared: 0.15
evaluation:
  n_splits: 3
  primary_metric: macro_f1
  min_valid_folds: 1
